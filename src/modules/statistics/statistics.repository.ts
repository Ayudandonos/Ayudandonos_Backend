/**
 * Statistics Repository — Skeleton (Fase 1)
 * Acceso a datos de estadísticas. Implementación en Fase 2.
 */
export class StatisticsRepository {
  // findAll, findById, etc. — Fase 2
}

export const statisticsRepository = new StatisticsRepository();
