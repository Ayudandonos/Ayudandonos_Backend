generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Los modelos se definirán en la Fase 2.
// La base de datos seguirá normalización 3FN con UUID como PK.
